name: Build Android APK

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Create local.properties & .env
      run: |
        echo "sdk.dir=$ANDROID_HOME" > local.properties
        echo "GEMINI_API_KEY=AIzaSyDummyKeyForBuildPurposeOnly12345" >> local.properties
        echo "API_KEY=AIzaSyDummyKeyForBuildPurposeOnly12345" >> local.properties
        
        if [ ! -f .env ]; then
          echo "GEMINI_API_KEY=AIzaSyDummyKeyForBuildPurposeOnly12345" > .env
          echo "API_KEY=AIzaSyDummyKeyForBuildPurposeOnly12345" >> .env
        fi

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3

    - name: Build APK with Gradle
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY || 'AIzaSyDummyKeyForBuildPurposeOnly12345' }}
        API_KEY: ${{ secrets.GEMINI_API_KEY || 'AIzaSyDummyKeyForBuildPurposeOnly12345' }}
      run: gradle assembleDebug

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Aplikasi-Belajar-Bahasa-Debug
        path: app/build/outputs/apk/debug/app-debug.apk
